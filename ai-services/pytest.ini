[pytest]
testpaths = tests
markers =
    tabpfn: requires the tabpfn + torch install (heavy); skipped if unavailable.
    live: hits a real external API (SEA-LION/Gemini); skipped without keys/network.
filterwarnings =
    ignore::DeprecationWarning
