# LUWAS data-pipeline — local-only ETL (NOT deployed). Python 3.12.
# Setup:
#   python3.12 -m venv data-pipeline/.venv
#   data-pipeline/.venv/bin/pip install -r data-pipeline/requirements.txt
geopandas==1.1.3
rasterio==1.5.0          # GDAL rasterization of NOAH hazard layers -> per-barangay exposure
shapely==2.1.2
pyogrio==0.12.1          # fast vector I/O backend for geopandas
pandas==3.0.3
numpy==2.4.6
SQLAlchemy==2.0.50
psycopg2-binary==2.9.12  # Postgres driver for Supabase
python-dotenv==1.2.2     # reads DATABASE_URL from repo-root .env.local
